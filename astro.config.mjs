// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://medico-web.pages.dev', // Coloca aquí la URL final de Cloudflare Pages
});