---
import { doctorConfig } from '../data/config.js';
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container footer-content">
    <div class="footer-info">
      <span class="doctor-name">{doctorConfig.nombre}</span>
      <p>{doctorConfig.especialidad + " | " + doctorConfig.localidad} </p>
    </div>

    <!-- Redes Sociales -->
    <div class="footer-socials">
      {doctorConfig.redesSociales.instagram && (
        <a href={doctorConfig.redesSociales.instagram} target="_blank" rel="noopener noreferrer" aria-label="Instagram">
          Instagram
        </a>
      )}
      {doctorConfig.redesSociales.tiktok && (
        <a href={doctorConfig.redesSociales.tiktok} target="_blank" rel="noopener noreferrer" aria-label="TikTok">
          TikTok
        </a>
      )}
      {doctorConfig.redesSociales.linkedin && (
        <a href={doctorConfig.redesSociales.linkedin} target="_blank" rel="noopener noreferrer" aria-label="LinkedIn">
          LinkedIn
        </a>
      )}
    </div>
    
    <div class="footer-copy">
      <p>&copy; {year} {doctorConfig.nombre}. Todos los derechos reservados.</p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background-color: #0f172a;
    color: #94a3b8;
    padding: 3rem 0;
    font-size: 0.875rem;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1.5rem;
  }

  .doctor-name {
    color: #ffffff;
    font-weight: 700;
    font-size: 1.125rem;
    display: block;
    margin-bottom: 0.25rem;
  }
  .footer-socials {
    display: flex;
    gap: 1.25rem;
  }

  .footer-socials a {
    color: #38bdf8;
    font-weight: 600;
    transition: color 0.2s;
  }

  .footer-socials a:hover {
    color: #ffffff;
  }

  @media (max-width: 640px) {
    .footer-content {
      flex-direction: column;
      text-align: center;
    }
  }
</style>