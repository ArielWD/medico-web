---
import BaseLayout from '../layout/BaseLayout.astro'
import Navbar from '../components/NavBar.astro'
import Hero from '../components/Hero.astro'
import Services from '../components/Services.astro'
import About from '../components/About.astro'
import Locations from '../components/Locations.astro'
import Appointment from '../components/Appointment.astro'
import Faq from '../components/Faq.astro'
import Footer from '../components/Footer.astro'
import Testimonials from '../components/Testimonials.astro'
// ⚠️ Solo para la demo — borrar estos imports al entregar el sitio a un cliente real.
import ForDoctors from '../components/ForDoctors.astro'
import DemoBanner from '../components/DemoBanner.astro'

import { nutricionistaConfig } from '../data/clientes/nutricionista.js'
---

<BaseLayout title="Lic. Andrea Márquez | Nutrición Clínica y Deportiva">
  <Navbar config={nutricionistaConfig} />
  <main class="container">
    <Hero config={nutricionistaConfig} />
	<Services config={nutricionistaConfig} />
	<About config={nutricionistaConfig} />
	<Testimonials config={nutricionistaConfig} />
	<Locations config={nutricionistaConfig} />
	<Appointment config={nutricionistaConfig} />
	<Faq config={nutricionistaConfig} />
  </main>
  <!-- ⚠️ Solo para la demo — borrar esta línea al entregar el sitio a un cliente real. -->
  <ForDoctors />
  <Footer config={nutricionistaConfig} />
  <!-- ⚠️ Solo para la demo — borrar esta línea al entregar el sitio a un cliente real. -->
  <DemoBanner />
</BaseLayout>

<style>
  .container {
	width: 100%;
    margin: 0 auto;
  }

  /* ⚠️ Solo para la demo — este padding compensa la altura del DemoBanner
     fijo abajo para que no tape el botón de "Agendar". Al borrar el banner,
     borra también este padding-bottom. */
  main {
    padding-bottom: 5rem;
  }
</style>
